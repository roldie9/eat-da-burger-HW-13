<img src="/assets/img/burger_ting.png" width="400px" id="burger_ting">

<h1>Eat-Da-Burger!</h1>

<h2>Add a burger to the menu!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">New Burger:</label>
    <input type="text" id="newBurger" name="name">
  </div>

  <button type="submit" class="addNewBurger">Add Burger</button>
</form>

<h2>Burger selection:</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured burgers:</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block}}
    {{/if}}
  {{/each}}
</ul>
